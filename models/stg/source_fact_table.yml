version: 2

sources:
  
  - name: smart_meters_data
    description: "smart meters are located in U.K."
    database: fivetran_data
    schema: pmv_files
    tables:
      - name: DIM_PMV_COMMSHUB
      - name: pmv_chsu
      - name: pmv_exception_override
      - name: pmv_adhoc


models:
  - name: stg__dim_commshub
    description: "Communications HUB table"
    columns:
      - name: COMMSHUB_KEY
        description: "This is a SURROGATE KEY"
        data_tests: 
          - unique